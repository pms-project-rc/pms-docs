# Guía de Despliegue

## Requisitos
- Docker y Docker Compose instalados.

## Pasos
1. Clonar el repositorio `pms-infra`.
2. Crear archivo `.env` con credenciales de base de datos.
3. Ejecutar `docker compose up -d`.
4. Verificar servicios en `localhost`.
